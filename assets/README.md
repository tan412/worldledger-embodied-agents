# Assets

The public release contains only schemas and metadata. Robot mesh bundles are downloaded on demand by `scripts/fetch_robot_models.py` from the pinned MuJoCo Menagerie revision and remain governed by their upstream licenses. They are not copied into this repository.
